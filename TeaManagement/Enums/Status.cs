namespace TeaManagement.Enums;

public enum Status
{
    Active = 1,
    Inactive = 2,
}

public enum RecStatus
{
    Active = 'A',
    Deleted = 'D',
}

public enum VoucherType
{
    Automatic = 1,
    Journal = 2
}